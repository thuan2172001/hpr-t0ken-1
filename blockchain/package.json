{
  "name": "hpr-t0ken",
  "version": "1.0.0",
  "description": "Haipro erc20 token",
  "main": "index.js",
  "scripts": {
    "compile": "yarn hardhat compile",
    "console": "yarn hardhat console --no-compile",
    "deploy": "yarn hardhat run scripts/deploy-token.js",
    "deploy:rinkeby": "yarn hardhat run scripts/deploy-token.js --network rinkeby",
    "deploy:tomotestnet": "yarn hardhat run scripts/deploy-token.js --network tomotestnet",
    "test": "yarn hardhat test"
  },
  "author": "haipro287",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.0.0",
    "dotenv": "^8.2.0",
    "hardhat": "^2.2.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.0.0"
  }
}
